# Supabase Konfiguration (DEV/PROD Trennung)
# Für lokale Entwicklung (localhost:3000)
SUPABASE_URL_DEV=https://deine-dev-projekt-id.supabase.co
SUPABASE_ANON_KEY_DEV=dein-dev-anon-public-key

# Für Produktion (Vercel)
SUPABASE_URL_PROD=https://deine-prod-projekt-id.supabase.co
SUPABASE_ANON_KEY_PROD=dein-prod-anon-public-key

# Fallback (wird nur verwendet, wenn DEV/PROD nicht gesetzt sind)
SUPABASE_URL=https://deine-projekt-id.supabase.co
SUPABASE_ANON_KEY=dein-anon-public-key

# Session Secret (für Produktion ändern!)
SESSION_SECRET=your-super-secret-session-key

# Umgebung
NODE_ENV=development

# Site URL (optional)
SITE_URL=http://localhost:3000
